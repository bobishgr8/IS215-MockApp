## Quickstart

```bash
cd backend

# Install dependencies
uv sync

# add GOOGLE_API_KEY to .env under backend/donation_matching_agent/

# run adk web server locally
adk web
# For local testing, access at http://127.0.0.1:8000. 
```